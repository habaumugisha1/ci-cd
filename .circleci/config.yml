version: 2.1
orbs:
  node: circleci/node@4.1.0
jobs:
  test:
    executor:
      name: node/default
      tag: 14.15.1
    steps:
    - checkout
    - run: npm install
    - run: npm run build
    - run: npm run test
    - run: npm run coverage
    - run: npm run make-badges
workflows:
version: 2.1
build:
  jobs:
    test